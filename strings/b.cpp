#include <bits/stdc++.h>
using namespace std;



int main(void){

	char in[1005];
	int dia, temp;
	char sol;
	while(scanf("%[^\n]s ,%d,%c,%d", in, &dia, &sol, &temp) != EOF){

		cout << "OK\n";

	}

}
